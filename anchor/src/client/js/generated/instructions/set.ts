/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { SIMPLECOLLECTION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_DISCRIMINATOR = new Uint8Array([
  198, 51, 53, 241, 116, 29, 126, 194,
]);

export function getSetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_DISCRIMINATOR);
}

export type SetInstruction<
  TProgram extends string = typeof SIMPLECOLLECTION_PROGRAM_ADDRESS,
  TAccountSimplecollection extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSimplecollection extends string
        ? WritableAccount<TAccountSimplecollection>
        : TAccountSimplecollection,
      ...TRemainingAccounts,
    ]
  >;

export type SetInstructionData = {
  discriminator: ReadonlyUint8Array;
  value: number;
};

export type SetInstructionDataArgs = { value: number };

export function getSetInstructionDataEncoder(): FixedSizeEncoder<SetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['value', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_DISCRIMINATOR })
  );
}

export function getSetInstructionDataDecoder(): FixedSizeDecoder<SetInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['value', getU8Decoder()],
  ]);
}

export function getSetInstructionDataCodec(): FixedSizeCodec<
  SetInstructionDataArgs,
  SetInstructionData
> {
  return combineCodec(
    getSetInstructionDataEncoder(),
    getSetInstructionDataDecoder()
  );
}

export type SetInput<TAccountSimplecollection extends string = string> = {
  simplecollection: Address<TAccountSimplecollection>;
  value: SetInstructionDataArgs['value'];
};

export function getSetInstruction<
  TAccountSimplecollection extends string,
  TProgramAddress extends Address = typeof SIMPLECOLLECTION_PROGRAM_ADDRESS,
>(
  input: SetInput<TAccountSimplecollection>,
  config?: { programAddress?: TProgramAddress }
): SetInstruction<TProgramAddress, TAccountSimplecollection> {
  // Program address.
  const programAddress = config?.programAddress ?? SIMPLECOLLECTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    simplecollection: { value: input.simplecollection ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.simplecollection)],
    programAddress,
    data: getSetInstructionDataEncoder().encode(args as SetInstructionDataArgs),
  } as SetInstruction<TProgramAddress, TAccountSimplecollection>;

  return instruction;
}

export type ParsedSetInstruction<
  TProgram extends string = typeof SIMPLECOLLECTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    simplecollection: TAccountMetas[0];
  };
  data: SetInstructionData;
};

export function parseSetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      simplecollection: getNextAccount(),
    },
    data: getSetInstructionDataDecoder().decode(instruction.data),
  };
}
